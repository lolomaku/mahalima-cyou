import{normalizeUrl}from"./platform.js";export const STORAGE_KEY="mahalima_custom_pins";export const APPS_SCRIPT_URL="https://mahalima-cyou.lolomaku.workers.dev/";export const CACHE_TTL_MINUTES=30;export const SHEET_CACHE_KEY="mahalima_sheet_cache";export function isConfigured(){return!APPS_SCRIPT_URL.includes("YOUR_DEPLOYMENT_ID")||(console.warn("[Mahalima config] APPS_SCRIPT_URL is still a placeholder.\nSighting fetch/submit will silently no-op or fail until js/config.js is filled in."),!1)}export const CATEGORY_COLOR={Concert:"#FF5964","Fan Gathering":"#FF5964","Billboard Sighting":"#FF5964","Stream Party":"#FF5964"};export const DEFAULT_COLOR="#FF5964";export function categoryColor(t){return CATEGORY_COLOR[t]||"#FF5964"}export function escapeHtml(t=""){const n=document.createElement("div");return n.textContent=t,n.innerHTML}export function rgbaFromHex(t,n){const r=t.replace("#","");return`rgba(${parseInt(r.substring(0,2),16)},${parseInt(r.substring(2,4),16)},${parseInt(r.substring(4,6),16)},${Math.max(0,n)})`}export function sightingKey(t){const n=normalizeUrl(t?.social_url||"");if(n)return`url:${n}`;const r=null!=t?.lat?Number(t.lat).toFixed(4):"",e=null!=t?.lng?Number(t.lng).toFixed(4):"",o=(t?.title||"").trim().toLowerCase();return`coord:${r}_${e}_${t?.date||""}_${o}`}